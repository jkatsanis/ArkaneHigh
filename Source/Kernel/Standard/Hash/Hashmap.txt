#include "Hashmap.h"

HashMap::HashMap() {
    // Initialize keys array to an invalid value
    for (int i = 0; i < HASHMAP_SIZE; ++i) {
        keys[i] = -1; // assuming -1 is not a valid key
        values[i] = 0; // initialize values to 0 (or any default value appropriate for your use case)
        occupied[i] = false; // initialize the occupied array to false
    }
}

void HashMap::insert(int key, int value) {
    int d = hash(4);
    Arkn::TextRenderer::WriteLine(d);
}

int HashMap::hash(int key) {
    return key % HASHMAP_SIZE;
}
